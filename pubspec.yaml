name: sigdet
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.0-42.1.beta <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+5
  image: ^4.1.3
  path_provider: ^2.1.1
  flutter_vision: ^1.1.4
  # opencv: ^1.0.4
  # opencv:
  #   git:
  #     url: https://github.com/sadreactonly/flutter_opencv.git
  #     ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/models/
